[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-teacher"
version = "0.1.0"
description = "A Digital Aristotle"
authors = [{name = "Dan", email = "dan@raymond.ch"}]

dependencies = [
    "ollama",
    "chromadb",
    "pysqlite3",
]
