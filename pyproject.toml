[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-teacher"
version = "0.1.0"
description = "A simple AI teacher project"
authors = [{name = "Dan", email = "dan@example.com"}]

dependencies = [
    "ollama",
    "chromadb",
]
